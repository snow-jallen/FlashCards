<Window x:Class="FlashCards.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FlashCards.Wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel>
        <TextBlock Text="LayoutTransform are used to putting all your text in a single textblock" />
        <TextBlock>
            <Run Text="bUT YOU CAN ALSO" />
            <Run Text="{Binding SingleWord}"
                 FontWeight="{Binding SingleWordFontWeight}" />
            <LineBreak />
            <Run Text="the rest of the sentense" />
        </TextBlock>          
    </StackPanel>
</Window>
